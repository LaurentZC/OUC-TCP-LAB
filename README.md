# Select Response

实现了选择响应协议，为发送方和接收方各维护一个窗口。
- 发送方的窗口存放了所有准备发送或者已经发送但是还没有确认的分组
- 接收方的窗口存放了所有准备接收或者已经接收但是还没有交付的分组

收发数据时，接收方逐个对所有正确收到的分组进行应答、对接收到的分组进行缓存，当左沿指针指向的包到达时对上层进行有效递交。
发送方为每个分组维护一个计时器，当计时器超时时重传对应的分组。